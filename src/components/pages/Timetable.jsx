// DEPRECATED — replaced by Upcoming.jsx. Kept only because the file system
// doesn't allow deletion from this environment; nothing imports it now.
import React from "react";
export default function Timetable() {
  return (
    <div className="card">
      <h2>Timetable moved</h2>
      <p className="muted">
        This page has been replaced by <strong>Upcoming</strong>. Use the nav
        on the left.
      </p>
    </div>
  );
}
